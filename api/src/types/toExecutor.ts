export type testCaseInput = {
    data_input: string;
    lang: string;
    question_id: string;
    typed_code: string;
    submission_id: string;
};
export type submitInput = {
    lang: string;
    question_id: string;
    typed_code: string;
    submission_id: string;
};